import usersTable from '../db/users';
import ridesTable from '../db/rides';
import requestsTable from '../db/requests';

setTimeout(usersTable, 500);
setTimeout(ridesTable, 1000);
setTimeout(requestsTable, 1000);